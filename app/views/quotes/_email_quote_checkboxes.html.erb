<% @document.client.contacts.each do |contact| %>
  <%= check_box_tag "quoteto[#{contact.id}][send]", 1, true, :id => "quoteto_#{contact.id}_send" %><%=h contact.firstname %> <%=h contact.lastname %>&nbsp;
<% end %>
<br/><%= check_box_tag "quoteto[#{current_user.id}][send]", 1, true, :id => "quoteto_#{current_user.id}_send" %>Send me a copy as well
