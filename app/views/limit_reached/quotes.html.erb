<div id="sidebar"></div>

<div id="content">

  <%= display_error %>
  <%= display_notice %>

  <%= render_cancel_button %>

  <h2>Quote limit reached</h2>
  
  <div class="limits">
    <p>Sorry, you have reached the limits of your current plan. You are using <strong><%= @app_config["app_name"] %> <%= @account.plan.name %></strong>, which allows you to send <strong><%= @account.plan.quotes %> quotes</strong> per monthly interval.</p>
    <p>You can either wait till <strong><%= fmt_date(@account.next_cycle_date) %></strong> for your next interval to begin, or <strong>upgrade</strong> to a different plan.</p>
  </div>
  
  <%= render :partial => 'account/plans' %>

</div>
